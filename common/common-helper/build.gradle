plugins {
    id 'java'
    id 'checkstyle'
    id 'jacoco'
    alias pluginLibs.plugins.spotbugs
}

apply from: "${rootDir}/java.gradle"

test {
    useJUnitPlatform()
    testLogging {
        events "passed", "skipped", "failed"
    }
}